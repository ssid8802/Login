package com.LoginSys.LoginSys.service;

import com.LoginSys.LoginSys.UsersModule;

public class userservice {

    public UsersModule registeruser(String username,String Password,String email)
    {
        if(username != null && Password != null)
        {
            UsersModule usersModule = new UsersModule();
            usermodule.setLogin(login);
            use
        }
    }
}
